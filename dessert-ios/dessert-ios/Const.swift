//Const.swift created on 29/06/2020.

import Foundation

struct GraphQL {
    static let SERVER_ERROR = "Server Error :("
    static let CONTACT_US = "Contact us if the error persists."
}

struct Login {
    static let INVALID_CREDS = "Invalid Credentials"
    static let CHECK_CREDS = "Please check your credentials."
    
    static let OOPS = "Ooops..."
    static let PROVIDE_CREDS = "You must provide an Email and a Password."
}

struct Logout {
    
}
